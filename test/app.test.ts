//A A A 
describe('Test in the app file', () => {

    test('should be true', () => {

        //1. Arrange
        const isActive: boolean = true;


        //2. Act
        // const isActive: boolean = true;

        //3. Assert
        expect(isActive).toBe(true);


    })

})